<?php
/**
 * LoginMathCheck
 *
 * A no-spam measure for Login/Register forms.
 * 
 * @package login
 * @subpackage register
 */

/* validator mode */
if (isset($hook)) {


} else {
    /* preset fields mode */
    $placeholderPrefix = $modx->getOption('placeholderPrefix',$scriptProperties,'');

}


